/** Not4 takes in 4-bit bus and returns the Not of each bit as a 4-bit bus */

// for i=0..3: out[i] = Not[i]

CHIP Not4 {
    IN in[4];
    OUT out[4];

    PARTS:
    Not(in=in[0], out=out[0]);
    Not(in=in[1], out=out[1]);
    Not(in=in[2], out=out[2]);
    Not(in=in[3], out=out[3]);
}